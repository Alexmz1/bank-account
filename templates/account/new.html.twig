{% extends 'base.html.twig' %}

{% block title %}Nouveau compte bancaire{% endblock %}

{% block body %}
<div class="flex min-h-screen bg-gray-100">
    
    {% include 'parts/left_bar.html.twig' %}
    
    <!-- Main Content -->
    <main class="flex-1 p-6">
        <div class="flex flex-col items-center justify-start w-full">
            <div class="sm:mx-auto sm:w-full sm:max-w-sm">
                <h2 class="mt-10 text-center text-2xl/9 font-bold tracking-tight text-gray-900">Ajouter un nouveau compte bancaire</h2>
            </div>
            <div class="sm:mx-auto sm:w-full sm:max-w-sm">
                {{ include('account/_form.html.twig') }}
            </div>
        </div>
    </main>
</div>
{% endblock %}
